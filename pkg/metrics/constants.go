package metrics

const ArgusNamespace = "argus"

const (
	// LMAPISubsystem subsystem for metrics around lm api calls
	LMAPISubsystem = "lm_api"
	// WorkerSubsystem worker related metrics
	WorkerSubsystem = "worker"
	// RunnerSubsystem runner related metrics
	RunnerSubsystem = "runner"

	// WatcherSubsystem watcher related metrics
	WatcherSubsystem = "watcher"

	// CacheSubsystem cache metrics
	CacheSubsystem = "cache"

	// SyncSubsystem sync related metrics
	SyncSubsystem = "sync"
)
